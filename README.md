# 🏢 AI Агент Недвижимости

Интерактивное веб-приложение для анализа инвестиций в недвижимость Дубая с использованием Claude AI.

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- API ключ Anthropic (получите на https://console.anthropic.com/)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка зависимостей сервера (в корневой папке)
npm install

# Установка зависимостей фронтенда
cd frontend
npm install
cd ..
```

### 2. Настройка API ключа

Создайте файл `.env` в корневой папке:

```bash
cp .env.example .env
```

Откройте `.env` и вставьте ваш API ключ:

```
ANTHROPIC_API_KEY=sk-ant-api03-ваш-ключ-здесь
```

### 3. Запуск приложения

**Терминал 1 - Запуск сервера:**
```bash
npm start
```
Сервер запустится на http://localhost:3001

**Терминал 2 - Запуск фронтенда:**
```bash
cd frontend
npm run dev
```
Фронтенд запустится на http://localhost:3000

### 4. Откройте браузер

Перейдите на http://localhost:3000

## 📁 Структура проекта

```
property-agent/
├── server.js           # Express сервер (API proxy)
├── package.json        # Зависимости сервера
├── .env.example        # Пример конфигурации
├── .env                # Ваш API ключ (создайте сами)
└── frontend/           # React приложение
    ├── src/
    │   ├── App.jsx     # Главный компонент
    │   ├── main.jsx    # Entry point
    │   └── index.css   # Стили (Tailwind)
    ├── index.html
    ├── package.json
    ├── vite.config.js
    ├── tailwind.config.js
    └── postcss.config.js
```

## 🎯 Функции

- **Обзор объекта** - оценка инвестиционной привлекательности
- **Новости района** - актуальные новости о Palm Jumeirah
- **Потенциал роста** - прогноз стоимости на 3-5 лет
- **Анализ рисков** - оценка рисков застройщика и рынка
- **Сравнение районов** - сравнение топ-районов Дубая
- **Сроки стройки** - анализ реалистичности дат сдачи
- **Свободные вопросы** - задайте любой вопрос ИИ-агенту

## ⚠️ Важно

- Информация носит справочный характер
- Не является финансовой консультацией
- Проверяйте данные из независимых источников

## 🔧 Troubleshooting

**Ошибка CORS:**
Убедитесь, что сервер запущен на порту 3001

**Ошибка API:**
Проверьте, что API ключ правильно указан в файле `.env`

**Кнопки не работают:**
1. Проверьте, что сервер запущен (`npm start`)
2. Проверьте консоль браузера на ошибки
3. Проверьте консоль сервера на ошибки

## 📝 Лицензия

MIT
